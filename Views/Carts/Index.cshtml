@model WebAppDemo3.Models.Book;
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    /*.card {
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            max-width: 300px;
            margin: auto;
            text-align: center;
            font-family: arial;
            width: 1200px;
            float: left;

        }

        .price {
            color: grey;
            font-size: 22px;
        }

        .card a {
            border: none;
            outline: 0;
            padding: 12px;
            color: white;
            background-color: #000;
            text-align: center;
            cursor: pointer;
            width: 100%;
            font-size: 18px;

        }

            .card button:hover {
                opacity: 0.7;
            }

            .list-book{
            width: 1200px;
            margin: auto;
            display: flex;
            flex-wrap: wrap;
            }
            .img{
                width:200px;
                height:200px;
                display:block;
                margin:20px;
            }
    */
    .card a:hover {
        opacity: 0.7;
    }

    .price {
        color: grey;
        font-size: 22px;
    }

    h1.title {
        font-size: 30px;
    }

    .card a {
        border: none;
        outline: 0;
        padding: 12px;
        color: white;
        background-color: #FF6633;
        text-align: center;
        cursor: pointer;
        width: 100%;
        font-size: 25px;
    }

    .list-book {
        width: 1200px;
        margin: auto;
        display: flex;
        flex-wrap: wrap;
    }

    .card {
        /* margin-top: 10px;
            margin-right: 10px;
            width: 120px;
            height: 200px;*/

        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        max-width: 300px;
        margin: 20px;
        text-align: center;
        font-family: arial;
        width: 1200px;
        float: left;
    }

    img {
        width: 100%;
        height: 100%;
        display: block;
        margin: auto;
        border-radius: 15px;
    }

    .title {
        display: block;
        font-size: 15px;
        font-weight: bold;
        text-align: center;
    }

    .viewBook, .addBook {
        wid
</style>

<div class="list-book">
    @foreach (var item in ViewBag.book)
    {
        <div class="card">
            <img src="@item.Image" />

            <h1 class="title">@item.Title</h1>

            <p class="price">$@item.Price</p>

            @* <a asp-controller="Carts" asp-action="addCart" asp-route-id="@item.BookId" class="addBook">Add order</a>*@

            <p>
                <a asp-controller="Carts" asp-action="addCart" asp-route-id="@item.BookId" class="addBook">
                    Add to Cart !
                </a>
            </p>
        </div>
    }


</div>
